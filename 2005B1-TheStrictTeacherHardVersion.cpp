#include <cstdio>

int main(){

    long t; scanf("%ld", &t);
    while(t--){
        long n, m, q; scanf("%ld %ld %ld", &n, &m, &q);
        std::vector<long> b(m); for(long p = 0; p < m; p++

    }
}
